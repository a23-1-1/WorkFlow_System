【仅粘贴到 USER / 用户消息】
本文件全文粘贴到 LLM 节点的 USER 输入框；勿粘贴到 SYSTEM，勿粘贴到「上下文（Context）」。

请根据以下「简历原文」，严格按系统提示中的 JSON Schema 提取结构化信息。

要求：
1. 只使用下方「简历原文」中的信息，不要编造。
2. 若「简历原文」为空或仅有空白/未替换的 {{#...#}}，在 JSON 根对象填写 error 说明原因，其余字段按 Schema 留空。
3. 若正文非空，至少提取 name、education 或 work_experience 中的一项；禁止 name、education、work_experience、skills 同时全空。
4. 无「姓名：」「教育背景：」等标签时：文首姓名、手机号、@邮箱、届别/学校→education、实习项目→work_experience、技术词→skills。
5. 只输出一个 JSON 对象，不要 Markdown 代码块。

【简历原文】
{{#文档提取器.text#}}

（变量说明：上方占位符须与画布一致。默认输出变量为 text → {{#文档提取器.text#}}；若改名为 extracted_text → {{#文档提取器.extracted_text#}}；节点名不同则改为 {{#你的节点名.变量名#}}。粘贴后运行 Trace，确认「【简历原文】」下方为真实正文，而非未替换的 {{#...#}}。）

【配置提醒】勿在 LLM「上下文」中重复添加同一文档提取器变量；正文只通过本 USER 模板传入即可。
【配置提醒】若上下文已挂变量导致橙字「要启用上下文功能…」，请删除上下文中的变量并留空，仅保留本 USER 中的 {{#文档提取器.text#}}。
