【仅粘贴到 USER / 用户消息】
本文件全文粘贴到 LLM 节点 USER 输入框；勿粘贴到 SYSTEM，勿仅依赖 Context。

请根据以下「岗位原文」，按系统提示提取结构化 JD 信息并仅输出 JSON。

要求：
1. 只使用下方岗位原文信息，不要编造。
2. 若岗位原文为空或仅为未替换占位符，返回带 error 的 JSON。
3. 只输出单个 JSON 对象，不要 Markdown。

【岗位原文】
{{#开始.jd_text#}}

（变量说明：若你使用文件提取器，请改为 {{#文档提取器_jd.text#}}；节点名与变量名需与画布一致。）
